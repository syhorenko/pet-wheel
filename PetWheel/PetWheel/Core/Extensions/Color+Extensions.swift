import SwiftUI

extension Color {
    static let petPrimary = Color("PetPrimary")
    static let petSecondary = Color("PetSecondary")
    static let petAccent = Color("PetAccent")
    static let petBackground = Color("PetBackground")

    static let wheelColors: [Color] = [
        .red, .orange, .yellow, .green,
        .blue, .purple, .pink, .cyan
    ]
}
